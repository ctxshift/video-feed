{
  "name": "video-feed",
  "version": "0.2.0",
  "description": "Context-aware video transcripts: verbatim speech, corrected against what is on screen.",
  "type": "module",
  "module": "src/cli.tsx",
  "scripts": {
    "embed": "bun run scripts/embed-asr.ts",
    "build": "bun run embed && bun build --compile --minify src/cli.tsx --outfile dist/vid",
    "dev": "bun run embed && bun run src/cli.tsx",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "commander": "^13.0.0",
    "ink": "^5.1.0",
    "ink-spinner": "^5.0.0",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^18.3.0",
    "typescript": "^5.7.0"
  }
}
